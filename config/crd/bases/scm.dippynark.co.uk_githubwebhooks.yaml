
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: githubwebhooks.scm.dippynark.co.uk
spec:
  group: scm.dippynark.co.uk
  names:
    kind: GitHubWebhook
    listKind: GitHubWebhookList
    plural: githubwebhooks
    singular: githubwebhook
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GitHubWebhook is the Schema for the githubwebhooks API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: GitHubWebhookSpec defines the desired state of GitHubWebhook
            properties:
              active:
                type: boolean
              contentType:
                description: 'Enum: application/x-www-form-urlencoded (default), application/json'
                type: string
              events:
                description: Events refers to Github events to subscribe to
                items:
                  type: string
                type: array
              id:
                description: 'ID holds the webhook''s GitHub ID. This allows edits
                  to be made. The ID is populated when the webhook is created or when
                  the specification matches with an existing webhook. The matching
                  criteria is as follows: events, url, content type,'
                format: int64
                type: integer
              insecureSSL:
                description: Active refers to status of the webhook for event deliveries.
                  https://developer.github.com/webhooks/creating/#active
                type: boolean
              payloadURL:
                description: URL to send webhook payload
                type: string
              repository:
                properties:
                  name:
                    type: string
                  owner:
                    type: string
                type: object
              secret:
                description: GitHub webhook secret https://developer.github.com/webhooks/securing/
                properties:
                  key:
                    description: The key of the secret to select from.  Must be a
                      valid secret key.
                    type: string
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                  optional:
                    description: Specify whether the Secret or its key must be defined
                    type: boolean
                required:
                - key
                type: object
            type: object
          status:
            description: GitHubWebhookStatus defines the observed state of GitHubWebhook
            properties:
              lastObservedSecretHash:
                type: string
              lastObservedUpdateTime:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
